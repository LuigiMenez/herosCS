import styled from "styled-components";

const SSaving = styled.section`
  .save {
    display: flex;
    justify-content: flex-end;
    /* justify-content: space-between; */
    margin: 0.5em 0 0 0;
  }
  .head {
    width: 21%;
    text-align: center;
  }
`;

export default SSaving;
