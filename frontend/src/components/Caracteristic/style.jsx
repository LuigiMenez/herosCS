import styled from "styled-components";

const SCarac = styled.section`
  .dataName {
    display: flex;
    justify-content: space-around;
  }

  .dataName p {
    margin: 0.5em 0 0 0;
    text-align: center;
    width: 15%;
  }
`;

export default SCarac;
